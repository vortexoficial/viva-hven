// Viva Haven — UI helpers (compat)
// Mantém o contrato de /js/ui.js, delegando para o core UI sem CSS inline.

export {
  toast,
  formatError,
  showError,
  setLoading,
  confirmDialog,
  openModal,
  setButtonLoading,
  bindGlobalErrorHandlers,
} from './core/ui.js';
